/**
 * - wifi & mqtt init, connect
 *      - event logger & led working (just running)
 * - ota check (& perform if needed)
 * - sdcard check
 * - sensormanager sensors activate
 */

int main() {
    
}

extern "C" void app_main() {
    main();
}